export function GlowField(_props: {
  className?: string;
  intensity?: "soft" | "default" | "strong";
}) {
  void _props;
  // Glows and gradient overlays have been removed in favor of solid surfaces.
  return null;
}
